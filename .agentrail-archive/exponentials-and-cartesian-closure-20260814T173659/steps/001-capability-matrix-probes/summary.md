Mapped CT065-CT084 to executable finite representations and measured their sw-MLPL prerequisites. Added a passing probe for four binary function rows, nested-selection evaluation, fixed 2-by-2 curry/uncurry round trips, four two-point subobjects and characteristic maps, slice/coslice records, flat dependent fibers, and polynomial container metadata. Documented that take accepts one axis/index pair per call, ragged arrays are rejected, and unequal fibers require flat values plus offsets/sizes. Generic enumeration, reshape, readable all, callable-returning curry, and static categorical guarantees remain ergonomic or scope boundaries, but none blocks CT065-CT070. Added the capability matrix and updated the concept inventory, plan, probe catalog, and both research logs. Full gate passes with 35 demos and 48 tests. Committed and pushed as 67a915c. Next: CT065-CT066 finite exponentials and evaluation for the two-point fixture. Blockers: none; do not generalize the bounded enumeration to arbitrary categories.