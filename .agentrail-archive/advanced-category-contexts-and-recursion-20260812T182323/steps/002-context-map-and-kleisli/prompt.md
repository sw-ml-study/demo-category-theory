Implement Lessons 12-13 from docs/plan.md using the verified concrete optional/error context: contextual map identity/composition and Kleisli composition for fallible data stages. Verify left identity, right identity, and associativity over finite inputs, and return stable failure witnesses. Add narrated CLI/web demos, reduced-motion SVGs, generated README previews, catalog/tests/docs updates, full gate, detailed commits, and push. If contextual function returns are blocked, record the probe and defer rather than inventing a large bridge.