Measure the current sw-MLPL surface needed for the advanced curriculum before implementing lessons. Probe reusable tagged optional/error payloads, returning and passing contextual records through function calls, nested payload equality, contextual mapping, short-circuit dispatch, and whether functions can return records without distortion. Define the smallest concrete result/optional representation in lib/context.mlpl with first-expression docstrings and readable names. Add law/witness tests and update docs/sw-mlpl-capabilities.md and docs/upstream-asks.md with verified evidence. Do not implement applicative, monadic, fold, unfold, optic, or adjunction lessons yet. Run the full gate, make detailed work and completion commits, and push both.