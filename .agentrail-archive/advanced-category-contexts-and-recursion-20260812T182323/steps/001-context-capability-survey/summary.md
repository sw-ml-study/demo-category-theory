Measured and implemented the advanced contextual-computation foundation. Tagged success/failure records retain nested payloads and equality; context map obeys identity/composition; functions can return contextual records through call; bind preserves those records and short-circuits earlier failures. Added lib/context.mlpl, a probe, 3 new tests, catalogs, and capability/upstream documentation. Full just check passed. Work commit f4ca871. Next: Step 2 implements Lessons 12-13: visible optional/error mapping and fallible Kleisli pipelines with identity/associativity laws and failure witnesses. It can proceed immediately. Known friction: ordinary numeric call results require one-element scalar normalization, while record returns must remain intact; static tag exhaustiveness remains unavailable but is not blocking.