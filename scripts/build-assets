#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
dest=${1:-"$repo_root/assets/previews"}
mkdir -p "$dest"
# Preview-producing demos are added in Step 2. Never invent placeholders.
echo "README previews written"
