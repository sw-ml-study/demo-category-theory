#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
mlpl=$("$repo_root/scripts/select-mlpl")
mkdir -p "$repo_root/out"
for demo in "$repo_root"/demos/[0-9][0-9]-*/*.mlpl; do
  [ -f "$demo" ] || continue
  echo "demo: ${demo#$repo_root/}"
  (cd "$repo_root" && "$mlpl" --source-dir "$repo_root" "$demo")
done
