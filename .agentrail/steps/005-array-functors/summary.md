Verified and implemented Lessons 07-08 with current sw-MLPL: each is a concrete array functor satisfying identity/composition over vectors and preserving measured matrix shapes. Added a stable fake-lift witness, shape-vs-entry preservation contrast, tests, CLI/web demos, synchronized SVGs, README previews, and capability documentation. Full just check passed. Work commit a46825c. Next: Step 6 attempts a concrete natural transformation over explicit array contexts/shapes. Prerequisites (each, composition, equality, commutative-square checker) are supported. Potential blocker: a generic natural transformation across arbitrary contexts would require generic functor/type-constructor abstraction; if concrete records/functions cannot teach it cleanly, the generic lesson will be deferred per plan rather than emulated.