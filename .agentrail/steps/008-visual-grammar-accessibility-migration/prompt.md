Apply research2 visual recommendations across existing authored SVG lessons: distinguish categorical objects/containers from element tokens, avoid red/green-only meaning with labels/shapes/dashes, label semantic arrows aggressively, add SVG title and desc, and use path/progressive animation for diagrams where it materially teaches the law. Update visual checks and generated assets/web files, document the future standard, full gate, commits, pushes.