Implement CT110-CT112 as Lesson 50 using one finite typed semantic port graph. Show sequential and tensor composition as connectivity data, render at least two visibly different planar embeddings with exactly the same checked semantic edges, and execute interchange using independently constructed sequential-then-tensor and tensor-then-sequential endpoints. Include type-mismatch, changed-connectivity, and broken-interchange witnesses. The visual must label ports and directed wires clearly while explaining that coordinate movement is only a bounded isotopy witness. Add docstring-first readable functions, tests, CLI/web leaves with consistent bound-once SVG display, README preview, catalogs, browser curriculum, quality manifest, and all plan/research/capability docs including research.txt and research2.txt. Run full pre-commit checks, make a detailed work commit and push, complete Agentrail with metadata commit and push, then proceed to cups/caps connectivity.